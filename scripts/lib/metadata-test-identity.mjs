/**
 * Re-export the MetadataStore fixture for smoke scripts.
 * Prefer importing from `@agentx/metadata` / packages/metadata dist in new code.
 */
export { createVerifiedTestIdentity } from "../../packages/metadata/dist/index.js";
